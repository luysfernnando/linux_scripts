version: 2

project_name: sysup

builds:
  - id: sysup
    dir: sysup
    main: .
    binary: sysup
    ldflags:
      - -s -w -X main.version=v{{ .Version }}
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64

archives:
  - id: sysup
    ids: [sysup]
    formats: [tar.gz]
    format_overrides:
      - goos: windows
        formats: [zip]
    name_template: >-
      {{ .ProjectName }}_{{ .Os }}_{{ .Arch }}

checksum:
  name_template: "checksums.txt"

release:
  github:
    owner: luysfernnando
    name: linux_scripts
