# Ignore Windows file system alternate data streams
*Zone.Identifier

# Backups gerados pelo install.sh
dotfiles/*.bak

# Binário compilado do sysup (build local) e output do GoReleaser
sysup/sysup
dist/